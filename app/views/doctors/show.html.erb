<h1><%= @doctor.name %></h1>
    <h2><%= @doctor.department %></h2>
    <h3><% @doctor.appointments.each do |app| %>
        <%= link_to app.patient.name, patient_path(app.patient) %> at
        <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
    <% end %></h3>