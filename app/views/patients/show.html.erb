<h1><%= @patient.name %></h1>
    <h3><% @patient.appointments.each do |app| %>
        <%= link_to app.doctor.name, doctor_path(app.doctor) %> at
        <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %>
    <% end %></h3>